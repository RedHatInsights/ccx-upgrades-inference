name: Unit Tests

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    uses: RedHatInsights/processing-tools/.github/workflows/pytests.yaml@master
    with:
      packages: ccx_upgrades_inference
      install_package: true
      cov_branch: true
    secrets: inherit
