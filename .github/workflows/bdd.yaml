name: BDD Tests

on:
  push:
    # branches: ["main", "master"]
  pull_request:

jobs:
  bdd:
    name: BDD tests
    uses: joselsegura/processing-tools/.github/workflows/bdd.yaml@urp-inference-adapt
    with:
      service: inference-service
